

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">

    <a class="navbar-brand" href="/">Zenbatu</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">

        <!-- Navbar will show as active whichever name string of the page is passed to it -->
        <div class="navbar-nav">
            <li class="nav-item <%=(typeof page === "string" && page === "home") ? "active" : "" %>">
                <a class="nav-item nav-link" href="/">Home <span class="sr-only">(current)</span></a>
            </li>

            <li class="nav-item <%=(typeof page === "string" && page === "add_asset") ? "active" : "" %>">
                <a class="nav-item nav-link" href="/add_asset_page">Add Asset</a>
            </li>
            <li class="nav-item <%=(typeof page === "string" && page === "get_asset") ? "active" : "" %>">
                <a class="nav-item nav-link" href="/get_asset_page">Get Asset</a>
            </li>
        </div>
    
    </div>
</nav>